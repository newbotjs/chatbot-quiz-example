# Chatbot to play a quiz. Works on the server side and in the browser

## Installation

1. Download and run `npm install`
2. Start the `newbot emulator` (play in terminal) or `newbot serve` (play in emulator or external platform)

## Unit Tests

`newbot test`